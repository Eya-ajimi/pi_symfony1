<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Ticket de commande InnoMall</title>
    <style>
        /* Style simplifié compatible avec Dompdf */
        body {
            font-family: Arial, Helvetica, sans-serif;
            font-size: 14px;
            line-height: 1.4;
            color: #333;
            margin: 0;
            padding: 20px;
        }

        .ticket-container {
            width: 100%;
            max-width: 800px;
            margin: 0 auto;
            border: 1px solid #cccccc;
        }

        .ticket-header {
            background-color: #3498db;
            color: white;
            padding: 20px;
            text-align: center;
        }

        .ticket-logo {
            margin-bottom: 15px;
            text-align: right;
        }

        .ticket-logo img {
            max-height: 60px;
        }

        .ticket-title {
            font-size: 24px;
            font-weight: bold;
            text-transform: uppercase;
            margin-bottom: 5px;
        }

        .shop-info {
            background-color: #f8f9fa;
            padding: 15px;
            border-bottom: 1px solid #dddddd;
        }

        .shop-info table {
            width: 100%;
        }

        .shop-info td {
            vertical-align: top;
        }

        .shop-name {
            font-size: 18px;
            font-weight: bold;
            color: #2c3e50;
        }

        .ticket-info {
            text-align: right;
        }

        .ticket-body {
            padding: 20px;
        }

        .product-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 25px;
        }

        .product-table th {
            background-color: #f2f6fa;
            color: #2c3e50;
            text-align: left;
            padding: 10px;
            border-bottom: 2px solid #dddddd;
        }

        .product-table td {
            padding: 10px;
            border-bottom: 1px solid #dddddd;
        }

        .total-table {
            width: 40%;
            margin-left: auto;
            border-collapse: collapse;
        }

        .total-table th {
            text-align: left;
            padding: 8px;
            font-weight: bold;
        }

        .total-table td {
            text-align: right;
            padding: 8px;
        }

        .total-row {
            background-color: #f8f9fa;
            font-weight: bold;
        }

        .ticket-footer {
            background-color: #f8f9fa;
            padding: 15px;
            border-top: 1px solid #dddddd;
        }

        .additional-info h3 {
            font-size: 16px;
            margin-bottom: 10px;
        }

        .copyright {
            text-align: center;
            font-size: 12px;
            color: #777;
            margin-top: 15px;
            padding-top: 10px;
            border-top: 1px solid #dddddd;
        }
    </style>
</head>
<body>
<div class="ticket-container">
    <div class="ticket-header">
        <table width="100%">
            <tr>
                <td width="60%">
                    <h1 class="ticket-title">Facture</h1>
                    <p>Centre commercial InnoMall</p>
                </td>
                <td width="40%" class="ticket-logo">
                    <!-- Utiliser le logo encodé en base64 -->
                    <img src="data:image/png;base64,{{ logoBase64 }}" alt="InnoMall Logo">
                </td>
            </tr>
        </table>
    </div>

    <div class="shop-info">
        <table width="100%">
            <tr>
                <td width="60%">
                    <p class="shop-name">{{ ticketData.shopName }}</p>
                    <p>Boutique InnoMall</p>
                </td>
                <td width="40%" class="ticket-info">
                    <p><b>N° Ticket:</b> {{ ticketData.ticketNumber }}</p>
                    <p><b>Date:</b> {{ "now"|date("d/m/Y") }}</p>
                </td>
            </tr>
        </table>
    </div>

    <div class="ticket-body">
        <table class="product-table">
            <thead>
            <tr>
                <th>Produit</th>
                <th>Description</th>
                <th>Quantité</th>
                <th>Prix unitaire</th>
                <th>Total</th>
            </tr>
            </thead>
            <tbody>
            {% for product in ticketData.products %}
                <tr>
                    <td>{{ product.nomProduit }}</td>
                    <td>{{ product.description }}</td>
                    <td>{{ product.quantity }}</td>
                    <td>{{ product.prix|number_format(2, ',', ' ') }} €</td>
                    <td>{{ product.total|number_format(2, ',', ' ') }} €</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <table class="total-table">
            <tr>
                <th>Total HT</th>
                <td>{{ (ticketData.totalCommande )|number_format(2, ',', ' ') }} €</td>
            </tr>
            <tr>
                <th>TVA </th>
                <td>0 €</td>
            </tr>
            <tr class="total-row">
                <th>Total TTC</th>
                <td>{{ ticketData.totalCommande|number_format(2, ',', ' ') }} €</td>
            </tr>
        </table>
    </div>

    <div class="ticket-footer">
        <div class="additional-info">
            <h3>Informations complémentaires</h3>
            <p>Merci pour votre commande! Vos articles seront disponibles pour retrait au magasin dans les prochaines 24 heures.</p>
            <p>Votre commande vous a fait gagner 100 points de fidélité.</p>
            <p>Pour toute question concernant votre commande, veuillez contacter directement la boutique.</p>
        </div>

        <div class="copyright">
            <p>InnoMall - Le centre commercial innovant - www.innomall.com</p>
            <p>{{ "now"|date("Y") }} © Tous droits réservés</p>
        </div>
    </div>
</div>
</body>
</html>