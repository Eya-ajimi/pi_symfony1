<!DOCTYPE html>
<html>
<head>
    <title>Paiement réussi - InnoMall</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
    <style>
        /* Styles de base */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background-color: #f0f5fa;
            color: #333;
            line-height: 1.6;
            min-height: 100vh;
        }

        .container {
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        /* En-tête de la page avec dégradé */
        .success-header {
            text-align: center;
            background: linear-gradient(135deg, #0a4b78, #2c87c5);
            padding: 40px 20px;
            border-radius: 12px;
            margin-bottom: 30px;
            box-shadow: 0 4px 20px rgba(10, 75, 120, 0.15);
            color: white;
        }

        .success-icon {
            font-size: 60px;
            margin-bottom: 15px;
            display: inline-block;
            background-color: white;
            color: #0a4b78;
            width: 100px;
            height: 100px;
            line-height: 100px;
            border-radius: 50%;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
        }

        .success-title {
            font-size: 32px;
            margin-bottom: 10px;
            font-weight: 700;
        }

        .success-subtitle {
            font-size: 18px;
            opacity: 0.9;
        }

        /* Conteneur principal avec layout à deux colonnes */
        .main-content {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
            margin-bottom: 30px;
        }

        /* Styles pour les cartes */
        .card {
            background-color: white;
            border-radius: 12px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.08);
            overflow: hidden;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.12);
        }

        .card-header {
            background: linear-gradient(to right, #0a4b78, #2c87c5);
            color: white;
            padding: 15px 20px;
            font-size: 18px;
            font-weight: 600;
            letter-spacing: 0.5px;
            display: flex;
            align-items: center;
        }

        .card-header i {
            margin-right: 10px;
        }

        .card-body {
            padding: 25px;
        }

        /* Confirmation de paiement */
        .payment-confirmed h2 {
            color: #0a4b78;
            margin-bottom: 15px;
            font-size: 24px;
            border-bottom: 2px solid #e1f0ff;
            padding-bottom: 10px;
        }

        .payment-confirmed p {
            color: #555;
            margin-bottom: 20px;
            font-size: 16px;
        }

        /* Points gagnés */
        .points-container {
            background: linear-gradient(135deg, #e1f0ff, #f0f9ff);
            border-radius: 10px;
            padding: 20px;
            margin: 20px 0;
            display: flex;
            align-items: center;
            border-left: 4px solid #2c87c5;
        }

        .points-icon {
            font-size: 28px;
            color: #2c87c5;
            margin-right: 15px;
            background-color: white;
            width: 60px;
            height: 60px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 50%;
            box-shadow: 0 2px 10px rgba(44, 135, 197, 0.2);
        }

        .points-text {
            flex-grow: 1;
        }

        .points-label {
            font-size: 14px;
            color: #555;
        }

        .points-value {
            font-size: 24px;
            font-weight: bold;
            color: #2c87c5;
        }

        /* Boutons */
        .buttons-group {
            display: flex;
            flex-wrap: wrap;
            gap: 15px;
            margin-top: 25px;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 12px 24px;
            text-decoration: none;
            font-weight: 600;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s ease;
            border: none;
            font-size: 16px;
            min-width: 180px;
        }

        .btn i {
            margin-right: 8px;
        }

        .btn-primary {
            background: linear-gradient(to right, #0a4b78, #2c87c5);
            color: white;
            box-shadow: 0 4px 10px rgba(10, 75, 120, 0.2);
        }

        .btn-primary:hover {
            background: linear-gradient(to right, #0a4b78, #3a9ad8);
            transform: translateY(-2px);
            box-shadow: 0 6px 15px rgba(10, 75, 120, 0.3);
        }

        .btn-outline {
            background-color: white;
            color: #0a4b78;
            border: 2px solid #0a4b78;
            box-shadow: 0 2px 5px rgba(10, 75, 120, 0.1);
        }

        .btn-outline:hover {
            background-color: #f0f7ff;
            transform: translateY(-2px);
            box-shadow: 0 4px 10px rgba(10, 75, 120, 0.15);
        }

        /* Résumé de commande */
        .summary-item {
            display: flex;
            justify-content: space-between;
            padding: 12px 0;
            border-bottom: 1px solid #eee;
        }

        .summary-item:last-child {
            border-bottom: none;
        }

        .summary-label {
            color: #666;
            font-size: 15px;
        }

        .summary-value {
            font-weight: 600;
            color: #0a4b78;
        }

        /* Shop list styling */
        .shop-list-container {
            height: 100%;
            display: flex;
            flex-direction: column;
        }

        .shop-list-header {
            margin-bottom: 20px;
        }

        .shop-list {
            max-height: 500px;
            overflow-y: auto;
            padding-right: 5px;
            /* Style de scrollbar personnalisé */
            scrollbar-width: thin;
            scrollbar-color: #2c87c5 #e1f0ff;
        }

        .shop-list::-webkit-scrollbar {
            width: 6px;
        }

        .shop-list::-webkit-scrollbar-track {
            background: #e1f0ff;
            border-radius: 10px;
        }

        .shop-list::-webkit-scrollbar-thumb {
            background-color: #2c87c5;
            border-radius: 10px;
        }

        .shop-item {
            background-color: white;
            border-radius: 10px;
            box-shadow: 0 3px 10px rgba(0, 0, 0, 0.08);
            margin-bottom: 15px;
            overflow: hidden;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .shop-item:hover {
            transform: translateY(-3px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.12);
        }

        .shop-header {
            background: linear-gradient(to right, #2c87c5, #4da3db);
            color: white;
            padding: 15px 20px;
            font-size: 16px;
            font-weight: 600;
            display: flex;
            align-items: center;
        }

        .shop-header i {
            margin-right: 10px;
        }

        .shop-content {
            padding: 15px 20px;
        }

        .shop-detail {
            display: flex;
            justify-content: space-between;
            padding: 8px 0;
            border-bottom: 1px solid #eee;
        }

        .shop-detail:last-child {
            border-bottom: none;
        }

        .shop-label {
            color: #666;
            font-size: 14px;
        }

        .shop-value {
            font-weight: 600;
            color: #2c87c5;
        }

        /* Footer */
        .footer {
            text-align: center;
            margin-top: 40px;
            color: #777;
            font-size: 14px;
            padding: 20px 0;
            border-top: 1px solid #dce8f2;
        }

        /* Pour les appareils mobiles */
        @media (max-width: 992px) {
            .main-content {
                grid-template-columns: 1fr;
            }

            .btn {
                min-width: 150px;
            }
        }

        @media (max-width: 576px) {
            .container {
                padding: 10px;
            }

            .success-header {
                padding: 30px 15px;
            }

            .success-icon {
                width: 80px;
                height: 80px;
                line-height: 80px;
                font-size: 40px;
            }

            .points-container {
                flex-direction: column;
                text-align: center;
            }

            .points-icon {
                margin-right: 0;
                margin-bottom: 15px;
            }

            .buttons-group {
                flex-direction: column;
            }

            .btn {
                width: 100%;
            }
        }
    </style>
</head>
<body>
<div class="container">
    <!-- En-tête avec icône de succès -->
    <div class="success-header">
        <div class="success-icon">
            <i class="fas fa-check"></i>
        </div>
        <h1 class="success-title">Paiement réussi</h1>
        <p class="success-subtitle">Votre commande a été traitée avec succès</p>
    </div>

    <!-- Conteneur principal avec layout à deux colonnes -->
    <div class="main-content">
        <!-- Colonne 1 : Confirmation + Commande -->
        <div class="left-column">
            <!-- Carte de confirmation -->
            <div class="card payment-confirmed">
                <div class="card-header">
                    <i class="fas fa-shield-alt"></i> Confirmation de paiement
                </div>
                <div class="card-body">
                    <h2>Paiement confirmé !</h2>
                    <p>Nous vous remercions pour votre achat. Les détails de votre commande ont été envoyés à votre adresse e-mail.</p>

                    <div class="points-container">
                        <div class="points-icon">
                            <i class="fas fa-gift"></i>
                        </div>
                        <div class="points-text">
                            <div class="points-label">Points de fidélité gagnés</div>
                            <div class="points-value">+ 100 points</div>
                        </div>
                    </div>

                    <div class="buttons-group">
                        <a href="{{ path('app_shops') }}" class="btn btn-primary">
                            <i class="fas fa-home"></i> Retour à l'accueil
                        </a>

                    </div>
                </div>
            </div>

            <!-- Résumé de commande -->
            <div class="card" style="margin-top: 30px;">
                <div class="card-header">
                    <i class="fas fa-receipt"></i> Récapitulatif de votre commande
                </div>
                <div class="card-body">
                    {#                    <div class="summary-item">#}
                    {#                        <div class="summary-label">Numéro de commande</div>#}
                    {#                        <div class="summary-value">#{{ commandeId }}</div>#}
                    {#                    </div>#}
                    <div class="summary-item">
                        <div class="summary-label">Date</div>
                        <div class="summary-value">{{ "now"|date("d/m/Y") }}</div>
                    </div>
                    <div class="summary-item">
                        <div class="summary-label">Statut</div>
                        <div class="summary-value" style="color: #28a745;">
                            <i class="fas fa-check-circle" style="margin-right: 5px;"></i>Payée
                        </div>
                    </div>
                    <div class="summary-item">
                        <div class="summary-label">Points de fidélité totaux</div>
                        <div class="summary-value">{{ points }} points</div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Colonne 2 : Liste des boutiques -->
        <div class="right-column">
            <div class="card shop-list-container">
                <div class="card-header">
                    <i class="fas fa-store"></i> Détails des boutiques
                </div>
                <div class="card-body">
                    <div class="shop-list-header">
                        <p>Liste des boutiques concernées par votre commande</p>
                    </div>

                    <div class="shop-list">
                        {% if ticketDataByShop is defined and ticketDataByShop|length > 0 %}
                            {% for shopId, ticketData in ticketDataByShop %}
                                <div class="shop-item">
                                    <div class="shop-header">
                                        <i class="fas fa-shopping-bag"></i> {{ ticketData.shopName }}
                                    </div>
                                    <div class="shop-content">
                                        <div class="shop-detail">
                                            <div class="shop-label">Numéro de ticket</div>
                                            <div class="shop-value">#{{ ticketData.ticketNumber }}</div>
                                        </div>
                                        <div class="shop-detail">
                                            <div class="shop-label">Total</div>
                                            <div class="shop-value">{{ ticketData.totalCommande|number_format(2, ',', ' ') }} €</div>
                                        </div>
                                        <div class="shop-detail">
                                            <div class="shop-label">Articles</div>
                                            <div class="shop-value">{{ ticketData.products|length }}</div>
                                        </div>
                                    </div>
                                </div>
                            {% endfor %}
                        {% else %}
                            <div class="shop-item" style="text-align: center; padding: 30px 20px;">
                                <i class="fas fa-info-circle" style="font-size: 40px; color: #b3d4fc; margin-bottom: 15px;"></i>
                                <p style="color: #777;">Aucune boutique associée à cette commande</p>
                            </div>
                        {% endif %}

                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <div class="footer">
        <p>Un e-mail de confirmation avec les détails de votre commande a été envoyé à votre adresse e-mail.</p>
        <p style="margin-top: 10px;">© {{ "now"|date("Y") }} InnoMall - Le centre commercial innovant</p>
    </div>
</div>
</body>
</html>